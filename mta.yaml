_schema-version: "3.2"
ID: productlist
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: productlist-destination-content
  type: com.sap.application.content
  requires:
  - name: productlist-destination-service
    parameters:
      content-target: true
  - name: productlist-repo-host
    parameters:
      service-key:
        name: productlist-repo-host-key
  - name: productlist-uaa
    parameters:
      service-key:
        name: productlist-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: productlist_html_repo_host
          ServiceInstanceName: productlist-html5-srv
          ServiceKeyName: productlist-repo-host-key
          sap.cloud.service: productlist
        - Authentication: OAuth2UserTokenExchange
          Name: productlist_uaa
          ServiceInstanceName: productlist-xsuaa-srv
          ServiceKeyName: productlist-uaa-key
          sap.cloud.service: productlist
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: productlist-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: productlist-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - productlist.zip
      name: productlist
      target-path: resources/
- name: productlist
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: productlist-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: productlist-destination-service
    service-plan: lite
- name: productlist-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: productlist-xsuaa-srv
    service-plan: application
- name: productlist-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: productlist-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
